How to Build and Run

Build the Docker image:

docker-compose build


Start the container:

docker-compose up


You should now be able to visit your React app in your browser at http://localhost:8080
.
